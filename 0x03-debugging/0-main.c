#include "main.h"

/**
 * positive_or_negative - funtion which tests for negative or postive integer
 * @i: integer to test
 * main - tests function that prints if integer is positive or negative
 *
 * Return: Always 0 (Success)
*/


int main(void)
{
int i;

if (i > 0)
positive_or_negative(i);

return (0);
}


