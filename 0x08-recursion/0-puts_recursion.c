#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * _puts_recursion - function to print a string
 * followed by a new line
 *
 * @s: pointer to string
*/

void _puts_recursion(char *s)
{
puts(s);
}
