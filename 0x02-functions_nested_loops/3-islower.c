#include "main.h"

/**
 * _islower(int c) - checks for lowercase characters
 * and returns 1 if lowercase, and 0 otherwise
 *
 * Description: This function checks for lowercase characters
 * main - Entry point
 * Return: Always 0 (Success)
*/

int _islower(int c)
{
if(_islower(c))
{
return (1);
}else
{
return (0);
}
}
